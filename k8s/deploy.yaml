apiVersion: apps/v1
kind: Deployment
metadata:
  name: project1
spec:
  selector:
    matchLabels:
      app: project11
  template:
    metadata:
      labels:
        app: project11
    spec:
      containers:
      - name: project1
        image: dexterquazi/app
        resources:
          limits:
            memory: "128Mi"
            cpu: "600m"
        ports:
        - containerPort: 8080
